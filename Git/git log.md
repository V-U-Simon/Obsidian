---
aliases: [ ]
created: 2022.04.25 10:05:49 pm
modified: 2022.04.26 8:09:40 pm
---

#application🛠/Git⏳
#🏃/show

>[!cite]- Source
>- ⭐ [diff working tree - other commits с классной диаграммой](https://365git.tumblr.com)



```Bash
git log          # в текущей ветке
git log -3       # три последних
git log --all    # со всеми ветками
git log --reflog # абсолютно все коммиты
git log <имя_ветки>  # коммиты одной ветки
git log --branches=* # коммиты всех веток

# модификаторы вида, показывают
-p, --patch      # патч для каждого коммита (git log -p -2)
--graph 	     # граф с ветвлениями и историей слияний
--oneline 	     # список коммитов в одну строку
--relative-date  # дату в относительном формате («2 weeks ago»)

--stat 	         # статистику измененных файлов для каждого коммита
--shortstat      # только строку с количеством изменений/вставок/удалений для команды - stat

--name-only      # список измененных файлов после информации о коммите
--name-status    # список файлов, которые добавлены/изменены/удалены
--abbrev-commit  # только несколько символов SHA-1 

# Поиск коммита
git log --author="<pattern>"
git log --grep="<pattern>" # поиск коммитов с сообщениями, соответствующими аргументу
git log <since>..<until> # коммиты в диапазоне значений < since > и < until >
git log <file> # коммиты, содержащие указанный файл
```



```Bash
# Показывает коммиты в альтернативном формате. 
--pretty=format
# Возможные варианты опций: 
# oneline, 
# short, 
# full, 
# fuller 
# format (можно указать свой формат)

git log --pretty=format:"%h - %an, %ar : %s"
git log --pretty=format:"%Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%cr) %C(bold blue)<%an>%Creset" --all --graph 

%H  # Хеш коммита
%h  # Сокращенный хеш коммита
%T  # Хеш дерева
%t  # Сокращенный хеш дерева
%P  # Хеш родителей
%p  # Сокращенный хеш родителей
%an # Имя автора
%ae # Электронная почта автора
%ad # Дата автора (формат даты можно задать опцией --date=option)
%ar # Относительная дата автора
%cn # Имя коммитера
%ce # Электронная почта коммитера
%cd # Дата коммитера
%cr # Относительная дата коммитера
%s  # Содержание
```
